meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email":"testeregister@email.com",
    "password": "register1234", 
    "name": "Register"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
